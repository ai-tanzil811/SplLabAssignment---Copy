#include<stdio.h>
int main()
{
    const float pi = 3.14;
    const float goldenRatio = 1.618;
    pritf("The value of pi is %f", pi);
    pritf("The value of golden ratio is %f", goldenRatio);
    return 0;
}